import {useEffect,useRef} from 'react';
export function Modal({open,onClose,onConfirm,busy}:{open:boolean,onClose:()=>void,onConfirm:()=>void,busy:boolean}){const ref=useRef<HTMLButtonElement>(null);useEffect(()=>{if(open)ref.current?.focus();const esc=(e:KeyboardEvent)=>e.key==='Escape'&&!busy&&onClose();document.addEventListener('keydown',esc);return()=>document.removeEventListener('keydown',esc)},[open,busy,onClose]);if(!open)return null;return <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/60 p-4" role="dialog" aria-modal="true" aria-labelledby="confirm-title"><div className="card max-w-lg"><h2 id="confirm-title" className="text-xl font-bold">Final confirmation / இறுதி உறுதிப்படுத்தல்</h2><p className="my-5">You are about to send this petition from your Gmail account to the selected government recipients. This action cannot be undone.</p><div className="flex justify-end gap-3"><button ref={ref} disabled={busy} className="btn-secondary" onClick={onClose}>Cancel</button><button disabled={busy} className="btn-primary" onClick={onConfirm}>{busy?'Sending…':'Send Petition'}</button></div></div></div>}
