# Replace with an approved postal lookup endpoint before production if required.
# The URL must contain the literal {pin} placeholder and return PostOffice entries.
PIN_LOOKUP_API_URL=https://api.postalpincode.in/pincode/{pin}
